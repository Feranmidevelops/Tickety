// API base URL. Override with VITE_API_URL in a .env file if the API runs elsewhere.
export const API_URL = import.meta.env.VITE_API_URL ?? 'http://localhost:5003';
export const TOKEN_KEY = 'tickety.token';
